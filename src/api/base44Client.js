// Legacy client file - backwards compatibility only
// All functionality moved to /src/services/

export const legacyClient = {
  entities: {},
  integrations: { Core: {} },
  auth: {}
};

export const client = legacyClient;
